# Copy to secrets.env and fill in. secrets.env is gitignored.
#
# One Google Cloud key with these APIs enabled:
#   Routes API · Places API (New) · Maps JavaScript API
#
# This key ships inside the app and can be read by anyone who has it.
# Restrict it to those APIs and set a daily quota cap on each one.
GOOGLE_MAPS_KEY=
