*.iml
.gradle/
local.properties
.idea/
build/
captures/
.externalNativeBuild/
.cxx/

# iOS
ios/build/
DerivedData/
*.xcuserstate
xcuserdata/

# secrets
secrets.env
dist/

# Play signing — never commit these
keystore.properties
*.jks
*.keystore

# Build output. tools/build.sh injects the keys from secrets.env into these,
# so they contain live credentials and must never be committed.
dist/
app/src/main/assets/index.html
ios/Resources/index.html

# built APKs — they carry the injected Google key
apk/
